name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10.14"

      - name: Install Poetry
        run: pip install poetry==1.8.3

      - name: Configure Poetry
        run: |
          poetry config virtualenvs.in-project true

      - name: Cache Poetry dependencies
        uses: actions/cache@v4
        with:
          path: |
            .venv
            ~/.cache/pip
          key: poetry-lint-${{ runner.os }}-${{ hashFiles('poetry.lock') }}
          restore-keys: |
            poetry-lint-${{ runner.os }}-

      - name: Install dependencies with lint extras
        run: poetry install --with lint

      - name: Run ruff linter
        run: poetry run ruff check

  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10.14"

      - name: Install Poetry
        run: pip install poetry==1.8.3

      - name: Configure Poetry
        run: |
          poetry config virtualenvs.in-project true

      - name: Cache Poetry dependencies
        uses: actions/cache@v4
        with:
          path: |
            .venv
            ~/.cache/pip
          key: poetry-test-${{ runner.os }}-${{ hashFiles('poetry.lock') }}
          restore-keys: |
            poetry-test-${{ runner.os }}-

      - name: Install dependencies with test extras
        run: poetry install --with test

      - name: Run pytest
        run: poetry run pytest
